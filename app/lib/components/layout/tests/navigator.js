function Navigator({ folders }) {
	folders = {
		title: '',
		id: '',
		children: {
			title: '',
			id: '',
		},
	};

	return;

	//have a side panel that links all the tests to a nav that can be clicked to scroll to the test
}

export default Navigator;
